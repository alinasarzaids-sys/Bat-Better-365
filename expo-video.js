// Stub to prevent expo-video's SimpleCache from conflicting on Android.
// expo-video is intentionally excluded — use react-native-webview for video playback.
export const VideoPlayer = null;
export const useVideoPlayer = () => null;
export default {};
